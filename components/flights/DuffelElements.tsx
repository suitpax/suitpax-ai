"use client"

// Placeholder for potential Duffel Elements integration.
// We keep this component minimal until we wire up specific elements like ancillaries.

export default function DuffelElementsPlaceholder() {
  return (
    <div className="rounded-xl border border-neutral-800 bg-neutral-950 p-4 text-neutral-400 text-sm">
      Duffel Elements ready. Connect specific components (seats/bags) when needed.
    </div>
  )
}